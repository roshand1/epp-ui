process.IS_SERVER_SIDE = true;
// Put in a stub so that the FormData node module doesn't explode the process

// global.FormData = {};

// global.HGEppUi = {};

// global.HG3 = {};
// global.fs = require("fs");

// require("babel-polyfill");

// var React = require('React!react')
// var ReactDOM = require('ReactDOM!react-dom')
// var ReactDOMServer = require('ReactDOMServer!react-dom/server');

var EppComponents = require('./serversideComponents.js');